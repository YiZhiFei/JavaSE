package com.njxxyizhifei.javase.day09.test003;
/**
 *  封装的步骤：
 *   1.所有的属性私有化，使用private关键字进行修饰，private表示私有的，修饰所有数据只能在本类中访问
 *   2。对外提供简单的操作入口，也就是说以后外部程序想要访问age属性，必须通过这些简单的入口进行访问：
 *   
 *   回想以下，一个属性通常访问的时候包括集中访问形式？
 *      - 第一种方式：想读取这个属性的值，读取get
 *      - 第二种方式：想修改这个属性的值，修改set
 */
public class User {
	
	//属性私有化
	private int age;

}
